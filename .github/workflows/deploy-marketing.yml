name: Deploy Marketing Site

on:
  push:
    branches: [main]
    paths:
      - "marketing-site/**"
      - "docker-compose.yml"
      - ".github/workflows/deploy-marketing.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy marketing container on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script_stop: true
          script: |
            set -e
            cd "${{ secrets.DEPLOY_PATH }}"
            git fetch origin main
            git checkout main
            git pull --ff-only origin main
            docker compose -f docker-compose-example.yml up -d --build marketing
            docker image prune -f
            docker compose -f docker-compose-example.yml ps marketing
